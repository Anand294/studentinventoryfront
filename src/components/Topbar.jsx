import { useState } from 'react'
import Icon from './Icon'
const notices = [{title:'Maintenance due',text:'2 assets need attention this week.'},{title:'Student added',text:'Priya added a new student record.'}]
export default function Topbar({ menuOpen, onMenu }) { const [notifications, setNotifications] = useState(false); const [profile, setProfile] = useState(false); return <header className="topbar"><div className="topbar-start">{!menuOpen && <button className="icon-button mobile-menu" onClick={onMenu} aria-label="Open navigation"><Icon name="menu"/></button>}<div className="page-context"><span>Workspace</span><strong>Student inventory</strong></div></div><div className="topbar-actions"><label className="search-box"><Icon name="search" size={18}/><input placeholder="Search anything..."/></label><div className="topbar-popover-wrap"><button className="icon-button notification-button" onClick={() => setNotifications(!notifications)}><Icon name="bell"/><i/></button>{notifications && <div className="topbar-popover notifications"><div className="popover-heading"><strong>Notifications</strong><button onClick={() => setNotifications(false)}><Icon name="close" size={16}/></button></div>{notices.map(n => <p key={n.title}><b>{n.title}</b><br/>{n.text}</p>)}<a href="#activity">View all activity</a></div>}</div><div className="topbar-popover-wrap profile-wrap"><button className="profile-button" onClick={() => setProfile(!profile)}><span>AS</span><div><b>Alex Smith</b><small>Administrator</small></div><Icon name="down" size={16}/></button>{profile && <div className="topbar-popover profile-menu"><b>Alex Smith</b><small>alex@northstar.edu</small><hr/><button>Account settings</button><button>Sign out</button></div>}</div></div></header> }
